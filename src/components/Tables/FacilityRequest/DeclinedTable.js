import React, { useState } from 'react'
import { useHistory } from 'react-router-dom';
import { Table, TableBody, TableCell, TableContainer, TableHead, TableRow, Paper, Grid, Typography, TablePagination, TableFooter } from '@material-ui/core';
import { makeStyles } from '@material-ui/core/styles';
import ViewEditDialog from './../../Dialog/ViewEditDialog';
import Axios from 'axios'
import ConfirmDialog from './../../Dialog/ConfirmDialog';
import SuccessDialog from './../../Dialog/SuccessDialog';
import Notification from './../../Dialog/Notification';
import LoginAuthPop from './../../Dialog/LoginAuthPop';
import PopUp from './../../Dialog/PopUp';
import { FaEdit } from 'react-icons/fa';
import Moment from "react-moment";
const useStyles = makeStyles((theme) => ({ table: { minWidth: 200, }, tableContainer: { maxWidth: 1175, borderRadius: 0, }, tableHeaderCell: { fontWeight: 'bold', backgroundColor: '#0061a8', color: theme.palette.getContrastText(theme.palette.primary.dark), fontFamily: "Montserrat, sans-serif", cursor: "pointer", lineHeight: "1.43", paddingTop: "15px", paddingBottom: "15px" }, tableCell: { fontFamily: "Montserrat, sans-serif", cursor: "pointer", }, avatar: { backgroundColor: theme.palette.primary.light, color: theme.palette.getContrastText(theme.palette.primary.light) }, name: { cursor: "pointer", fontWeight: 'bold', color: "#2e4a56", fontFamily: "Montserrat, sans-serif" }, residentIdTitle: { fontSize: "15px" }, status: { fontWeight: 'bold', fontSize: '0.75rem', color: 'white', backgroundColor: 'grey', borderRadius: 5, padding: '3px 10px', display: 'inline-block', fontFamily: "Montserrat, sans-serif", }, pagination: { overflow: "hidden" } }))
export default function DeclinedTable({ data }) {
    Axios.defaults.withCredentials = true;
    const classes = useStyles(), history = useHistory(), [page, setPage] = useState(0), [rowsPerPage, setRowsPerPage] = useState(5), [viewEditDialog, setViewEditDialog] = useState({ isOpen: !1, title: "", subtitle: "", yesButton: "", noButton: "" }), [confirmDialog, setConfirmDialog] = useState({ isOpen: !1, title: "", subtitle: "", yesButton: "", noButton: "" }), [successDialog, setSuccessDialog] = useState({ isOpen: !1, title: "", subtitle: "", yesButton: "", noButton: "" }), [errorDialog, setErrorDialog] = useState({ isOpen: !1, title: "", subtitle: "", yesButton: "", noButton: "" }), [notify, setNotify] = useState({ isOpen: !1, message: "", type: "" }), [full_name, setFullName] = useState(""), [address, setAddress] = useState(""), [contact, setContact] = useState(""), [date_need, setDateNeed] = useState(""), [item, setItem] = useState(""), [date_requested, setDateRequested] = useState(""), [status, setStatus] = useState(""), [purpose, setPurpose] = useState(""), [quantity, setQuantity] = useState(""), [date_declined, setDateDeclined] = useState(""), [request_facilityId, setRequestFacilityId] = useState(""), [date_borrowed, setDateBorrowed] = useState(""), [remarks, setRemarks] = useState(""), [location, setLocation] = useState(""), [openPopup, setOpenPopup] = useState(!1), handleChangePage = (e, t) => { setPage(t) }, refresh = () => { setSuccessDialog({ ...successDialog, isOpen: !1 }); history.push("/Reservations-&-Requests") }, handleChangeRowsPerPage = e => { setRowsPerPage(+e.target.value); setPage(0) };
    const openInPopup = (remarks, date_declined, date_borrowed, req_facility_id, address, contact, date_need, item, location, quantity, date_requested, status, purpose, suffix, first_name, middle_name, last_name) => { setOpenPopup(true); setRequestFacilityId(req_facility_id); setFullName(first_name + " " + (middle_name === null ? " " : middle_name) + " " + last_name + " " + (suffix === null ? " " : suffix)); setAddress(address); setContact(contact); setDateNeed(date_need); setItem(item); setLocation(location); setQuantity(quantity); setDateRequested(date_requested); setStatus(status); setPurpose(purpose); setDateBorrowed(date_borrowed); setDateDeclined(date_declined); setRemarks(remarks); }
    const delete_request_confirm = () => { setConfirmDialog({ ...confirmDialog, isOpen: false }); Axios.delete(`http://localhost:3001/deleteFacilityRequest/${request_facilityId}`).then((response) => { if (response.data.err) { setErrorDialog({ isOpen: true, title: response.data.err, subtitle: "Please check your connection and try again.", noButton: (<button onClick={() => setErrorDialog({ ...errorDialog, isOpen: false })} className="alert_backBtn"> Back </button>), }); } else if (response.data.success) { setSuccessDialog({ isOpen: true, title: "Delete Facility Request", subtitle: response.data.success, noButton: <button onClick={() => refresh()} className="alert_backBtn">Back</button> }) } else { setErrorDialog({ isOpen: true, title: "Input Error!", subtitle: "File too small, please select a file greater than 200kb.", noButton: (<button onClick={() => setErrorDialog({ ...errorDialog, isOpen: false })} className="alert_backBtn"> Back </button>), }); } }) }
    const delete_request = () => { setConfirmDialog({ isOpen: true, title: "Confirmation", subtitle: "Are you sure you want to delete this request? This can't be undone.", noButton: <button onClick={() => setConfirmDialog({ ...confirmDialog, isOpen: false })} className="alert_noBtn">No</button>, yesButton: <button onClick={delete_request_confirm} className="alert_yesBtn"> Yes </button> }) }
    return (
        <div>
            <ViewEditDialog viewEditDialog={viewEditDialog} setViewEditDialog={setViewEditDialog} />
            <ConfirmDialog confirmDialog={confirmDialog} setConfirmDialog={setConfirmDialog} />
            <SuccessDialog successDialog={successDialog} setSuccessDialog={setSuccessDialog} />
            <LoginAuthPop errorDialog={errorDialog} setErrorDialog={setErrorDialog} />
            <Notification notify={notify} setNotify={setNotify} />
            <PopUp title="Declined Facility Request" openPopup={openPopup} setOpenPopup={setOpenPopup}> <div className="wrapper" id="wrapper_profile"> <div className="forms" id="inputfields_profile"> <div className="input_fields"> <label>Full Name:<span className="text-danger">*</span></label> <input type="text" className="inputs" autoComplete="off" disabled value={full_name} required onChange={(e) => setFullName(e.target.value)} /> </div><div className="input_fields"> <label>Address:<span className="text-danger">*</span></label> <textarea type="text" className="textarea" required autoComplete="off" disabled value={address} onChange={(e) => setAddress(e.target.value)} /> </div><div className="input_fields"> <label>Contact:<span className="text-danger">*</span></label> <input type="text" className="inputs" required autoComplete="off" disabled value={contact} onChange={(e) => setContact(e.target.value)} /> </div><div className="input_fields"> <label>Date Need</label> <p className="inputs"><Moment format={"MMMM DD, YYYY"} date={date_need} /></p></div><div className="input_fields"> <label>Facility:<span className="text-danger">*</span></label> <input type="text" className="inputs" required autoComplete="off" disabled value={item} onChange={(e) => setItem(e.target.value)} /> </div>{location ? <div className="input_fields"> <label>Facility Location:<span className="text-danger">*</span></label> <input type="text" className="inputs" required autoComplete="off" disabled defaultValue={location} onChange={(e) => setLocation(e.target.value)} /> </div> : ""}<div className="input_fields"> <label>Quantity:<span className="text-danger">*</span></label> <input type="text" className="inputs" required autoComplete="off" disabled value={quantity} onChange={(e) => setQuantity(e.target.value)} /> </div><div className="input_fields"> <label>Date Requested</label> <p className="inputs"><Moment format={"MMMM DD, YYYY"} date={date_requested} /></p></div><div className="input_fields"> <label>Date Declined</label> <p className="inputs"><Moment format={"MMMM DD, YYYY"} date={date_declined} /></p></div><div className="input_fields"> <label>Status:<span className="text-danger">*</span></label> <input type="text" className="inputs" required autoComplete="off" disabled value={status} onChange={(e) => setStatus(e.target.value)} /> </div><div className="input_fields"> <label>Purpose:<span className="text-danger">*</span></label> <input type="text" className="inputs" required autoComplete="off" disabled value={purpose} onChange={(e) => setPurpose(e.target.value)} /> </div><div className="input_fields"> <label>Remarks:<span className="text-danger">*</span></label> <textarea type="text" className="textarea" required autoComplete="off" disabled value={remarks} onChange={(e) => setRemarks(e.target.value)} /> </div></div></div><div className="add_resident_container_footer" id="mobile_hidden"> <div className="wrapper" id="wrapper_profile"> <div className="forms" id="inputfields_profile"> <div className="input_fields" > <input type="submit" value="Delete Request" className="editProfile_btn" onClick={delete_request} /> </div></div></div></div></PopUp>
            <TableContainer component={Paper} className={classes.tableContainer}> <Table className={classes.table} aria-label="simple table"> <TableHead> <TableRow> <TableCell className={classes.tableHeaderCell}>Last Name</TableCell> <TableCell className={classes.tableHeaderCell}>First Name</TableCell> <TableCell className={classes.tableHeaderCell}>Middle Name</TableCell> <TableCell className={classes.tableHeaderCell}>Facility</TableCell> <TableCell className={classes.tableHeaderCell}>Status</TableCell> <TableCell className={classes.tableHeaderCell} id="mobile_hidden">Action</TableCell> </TableRow> </TableHead> <TableBody>{data.slice(page * rowsPerPage, page * rowsPerPage + rowsPerPage).map((row) => { return (<TableRow > <TableCell style={{ cursor: 'pointer' }} onClick={() => openInPopup(row.remarks, row.date_declined, row.date_borrowed, row.req_facility_id, row.address, row.contact, row.date_need, row.item, row.facility_location, row.quantity, row.date_requested, row.status, row.purpose, row.suffix, row.first_name, row.middle_name, row.last_name)} id="last_name"> <Grid container> <Grid item lg={10}> <Typography className={classes.name} style={{ cursor: 'pointer' }}>{row.last_name}</Typography> </Grid> </Grid> </TableCell> <TableCell style={{ cursor: 'pointer' }} onClick={() => openInPopup(row.remarks, row.date_declined, row.date_borrowed, row.req_facility_id, row.address, row.contact, row.date_need, row.item, row.facility_location, row.quantity, row.date_requested, row.item_status, row.purpose, row.suffix, row.first_name, row.middle_name, row.last_name)}> <Grid container> <Grid item lg={10}> <Typography className={classes.name} style={{ cursor: 'pointer' }}>{row.first_name}</Typography> </Grid> </Grid> </TableCell> <TableCell style={{ cursor: 'pointer' }} onClick={() => openInPopup(row.remarks, row.date_declined, row.date_borrowed, row.req_facility_id, row.address, row.contact, row.date_need, row.item, row.facility_location, row.quantity, row.date_requested, row.item_status, row.purpose, row.suffix, row.first_name, row.middle_name, row.last_name)}> <Grid container> <Grid item lg={10}> <Typography className={classes.name} style={{ cursor: 'pointer' }}>{row.middle_name}</Typography> </Grid> </Grid> </TableCell> <TableCell className={classes.tableCell}>{row.item}</TableCell> <TableCell> <Typography className={classes.status} style={{ backgroundColor: ((row.item_status === "Borrowed" && 'green') || (row.item_status === "Pending" && 'red') || (row.item_status === "Returned" && '#0061a8') || (row.item_status === "Declined" && 'red')) }}>{row.item_status}</Typography></TableCell> <TableCell id="mobile_hidden"> <button className="view_edit" onClick={() => openInPopup(row.remarks, row.date_declined, row.date_borrowed, row.req_facility_id, row.address, row.contact, row.date_need, row.item, row.facility_location, row.quantity, row.date_requested, row.item_status, row.purpose, row.suffix, row.first_name, row.middle_name, row.last_name)}><FaEdit /></button> </TableCell> </TableRow>) })}</TableBody> <TableFooter> <TablePagination component="span" rowsPerPageOptions={[5, 10, 15]} count={data.length} rowsPerPage={rowsPerPage} page={page} onPageChange={handleChangePage} onRowsPerPageChange={handleChangeRowsPerPage} /> </TableFooter> </Table> </TableContainer>
        </div>
    )
}